# Asset Inventory – app (AuditLab)

**Generated:** 2026-03-02 (Deep Scan – Step 4)

## Asset locations (mobile patterns)

| Location | Content |
|----------|---------|
| `AuditLab/Assets.xcassets` | App asset catalog. |
| `AuditLab/Assets.xcassets/AppIcon.appiconset` | App icon set. |
| `AuditLab/Assets.xcassets/AccentColor.colorset` | Accent color. |

## Formats

- Asset catalog: `.appiconset`, `.colorset` (standard Xcode).
- Figures in papers: URLs inside parsed `ReadPack` (Fig.url); rendered in `FigurePanelView`.

## Summary

- **Asset catalog:** 2 entries (AppIcon, AccentColor).
- **No separate** `Resources/`, `res/`, or `images/` in app target; PDFs and figure assets are user-provided or extracted at runtime.
